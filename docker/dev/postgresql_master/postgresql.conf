listen_addresses = '*'
wal_level = replica
max_wal_senders = 3
wal_keep_size = 128MB  
max_wal_size = 1GB  
archive_mode = on
archive_command = 'cp %p /var/lib/postgresql/data/archive/%f'
hot_standby = on
hba_file = '/etc/postgresql/pg_hba.conf'
